{
  "name": "pase-guardia-functions",
  "description": "Cloud Functions for PaseGuardia",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin":     "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
